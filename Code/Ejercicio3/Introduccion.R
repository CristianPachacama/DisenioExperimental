# -------------- Diseño Cuadrado Latino ----------------
withMathJax("
En el diseño en cuadro latino (DCL) se controlan dos factores de bloque y se estudia
un factor de tratamientos, por lo que se tienen cuatro fuentes de variabilidad que
pueden afectar la respuesta observada, éstas son: los tratamientos, el factor de bloque
I (columnas), el factor de bloque II (renglones) y el error aleatorio. El modelo 
estadístico para describir el comportamiento de las observaciones está dado por:

$$Y_{ijl} = \\mu + \\tau_i + \\gamma_j + \\delta_l + e_{ijl}$$

donde \\(Y_{ijl}\\) es la observación del tratamiento \\(i\\), en el nivel \\(j\\) del factor renglón y en el
nivel \\(l\\) del factor columna; \\(e_{ijl}\\) es el error atribuible a dicha observación.
")
