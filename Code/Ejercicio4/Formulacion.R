# -------------- DiseÃ±o Cuadrado Greco-latino ----------------
tags$div(
withMathJax("
La variabilidad total presente en los datos se puede partir de
la manera usual como:

$$SC_T = SC_{TRAT} + SC_{B1} + SC_{B2} + SC_{B3} + SC_E$$

donde las sumas \\(SC_{B1}\\) , \\(SC_{B2}\\) y \\(SC_{B3}\\) miden la variabilidad debida a los facto res de
bloque renglón, columna y de letras griegas, respectivamente. Para \\(k\\) tratamientos,
los grados de libertad correspondientes a cada suma son:
$$k^2 - 1 = ( k - 1 ) + ( k - 1 ) + ( k - 1 ) + ( k- 3 )( k- 1 )$$

")
,p(img(src="Disenios/disenio3.png",width="620"),align="center")

)